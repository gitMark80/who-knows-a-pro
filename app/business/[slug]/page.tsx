import { notFound } from 'next/navigation'; import { ArrowLeft, BadgeCheck, ExternalLink, Globe2, MapPin, Phone, ShieldCheck } from 'lucide-react';
import { Header } from '@/components/site/header'; import { Footer } from '@/components/site/footer'; import { getBusiness } from '@/db/runtime';
export const dynamic='force-dynamic';
export default async function BusinessPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;let b=null;try{b=await getBusiness(slug)}catch{}if(!b)notFound();let photos:string[]=[];if(b.tier!=='free')try{photos=JSON.parse(b.photo_urls||'[]')}catch{}
const jsonLd={"@context":"https://schema.org","@type":"LocalBusiness",name:b.name,url:b.website,description:b.summary,telephone:b.phone||undefined,address:{"@type":"PostalAddress",addressLocality:b.location}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,'\\u003c')}}/><Header/><main className="bg-[#f5f7fa]"><div className="mx-auto max-w-5xl px-5 py-10 sm:px-8"><a href={`/${b.region}/${b.trade}`} className="inline-flex items-center gap-1 text-sm font-bold text-slate-600"><ArrowLeft className="size-4"/>Back to results</a><article className="mt-6 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-xl shadow-slate-200/50">{photos.length>0&&<div className="grid h-64 grid-cols-2 gap-1 bg-slate-100 sm:h-80">{photos.slice(0,3).map((p,i)=><img key={p} src={p} alt={`${b.name} business photo ${i+1}`} className={`h-full w-full object-cover ${i===0?'row-span-2':''}`}/>)}</div>}<div className="grid gap-9 p-6 sm:p-9 md:grid-cols-[1fr_280px]"><div><div className="flex items-center gap-4">{b.logo_url&&b.tier!=='free'?<div className="grid size-16 shrink-0 place-items-center overflow-hidden rounded-2xl bg-[#eaf0f6]"><img src={b.logo_url} alt={`${b.name} logo`} className="size-full object-cover"/></div>:null}<div><div className="flex items-center gap-1.5 text-xs font-black uppercase tracking-wider text-[#27715c]"><BadgeCheck className="size-4"/>{b.owner_email?'Claimed business':'Public listing'}</div><h1 className="mt-1 text-3xl font-black tracking-tight sm:text-4xl">{b.name}</h1></div></div><p className="mt-6 flex items-center gap-2 text-slate-500"><MapPin className="size-4 text-[#ec7d2c]"/>{b.location}</p><p className="mt-6 text-base leading-8 text-slate-700">{b.summary}</p><div className="mt-8 rounded-2xl border border-slate-200 bg-slate-50 p-5"><h2 className="font-extrabold">Before you hire</h2><p className="mt-2 text-sm leading-6 text-slate-600">Confirm price, availability, licensing, insurance, and project details directly with the business.</p></div></div><aside className="h-fit rounded-2xl bg-[#142c4c] p-5 text-white"><h2 className="text-lg font-extrabold">Contact this pro</h2><div className="mt-5 space-y-3">{b.phone&&<a href={`tel:${b.phone}`} className="flex items-center gap-2 rounded-xl bg-white/10 p-3 font-bold"><Phone className="size-4"/>{b.phone}</a>}<a href={b.website} target="_blank" rel="noopener noreferrer" aria-label={`Visit ${b.name} official website`} className="flex items-center gap-2 rounded-xl bg-[#ec7d2c] p-3 font-bold"><Globe2 className="size-4"/>Visit official website <ExternalLink className="ml-auto size-3.5"/></a></div><p className="mt-3 break-all text-xs text-white/65">{b.website}</p><div className="mt-5 flex items-start gap-2 border-t border-white/15 pt-5 text-xs leading-5 text-white/60"><ShieldCheck className="mt-0.5 size-4 shrink-0"/>Directory profiles are informational and are not endorsements.</div></aside></div></article>{!b.owner_email&&<div className="mt-5 flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-slate-200 bg-white p-5"><div><p className="font-extrabold">Own this business?</p><p className="text-sm text-slate-500">Claim this profile to update its information.</p></div><a href={`/claim?business=${encodeURIComponent(b.id)}`} className="rounded-xl bg-[#ec7d2c] px-5 py-3 font-extrabold text-white">Claim this business</a></div>}</div></main><Footer/></>}
